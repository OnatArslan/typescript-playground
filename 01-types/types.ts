const today = new Date();
console.log(today);

const person = {
  age: 20,
};

class Color {}

const red = new Color();

const abbas = 'Abbas' as string;

abbas.padStart(10);
console.log(abbas.length);
